import createTheme from "@sonnat/ui/styles/createTheme";

const theme = createTheme({
  typography: { ltrFontFamily: "Roboto", monospaceFontFamily: "Roboto Mono" },
  direction: "ltr"
});

export default theme;
